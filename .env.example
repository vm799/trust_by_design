# JobProof v2 - Environment Configuration
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your Supabase credentials from https://supabase.com/dashboard
# 3. Restart your dev server: npm run dev
#
# Get these values from: Supabase Dashboard → Settings → API

# Supabase Project URL (e.g., https://abcdefghijk.supabase.co)
VITE_SUPABASE_URL=

# Supabase Anonymous Public Key (find under "Project API keys" → "anon public")
VITE_SUPABASE_ANON_KEY=

# Stripe Configuration (Test Mode)
# Get these from: Stripe Dashboard → Developers → API Keys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx

# Stripe Price IDs (Test Mode)
# Create products in Stripe Dashboard, copy Price IDs
VITE_STRIPE_PRICE_TEAM_MONTHLY=price_xxx
VITE_STRIPE_PRICE_TEAM_ANNUAL=price_xxx
VITE_STRIPE_PRICE_AGENCY_MONTHLY=price_xxx
VITE_STRIPE_PRICE_AGENCY_ANNUAL=price_xxx

# Stripe Webhook Secret (from Webhooks tab)
STRIPE_WEBHOOK_SECRET=whsec_xxx

# What3Words API Key (for location addressing)
# Get from: https://what3words.com/select-plan → Developer → API Keys
VITE_W3W_API_KEY=

# Resend API Key (for email delivery - job reports, notifications)
# CRITICAL: Without this key, email reports will NOT be sent!
# Get from: https://resend.com/api-keys
# Add this to Supabase Edge Function secrets: supabase secrets set RESEND_API_KEY=re_xxx
RESEND_API_KEY=re_xxx

# Optional: Remove this once you've configured Supabase (unused in current version)
# GEMINI_API_KEY=

# ============================================================================
# ENVIRONMENT MODE
# ============================================================================
# Controls auth UI: 'staging' shows password login, production shows magic link only.
# Dev mode is auto-detected via localhost — no need to set this locally.
# Set to 'staging' on your staging/preview deployment.
# VITE_APP_ENV=staging

# ============================================================================
# DEV/STAGING TEST USERS (Quick Role Switching)
# ============================================================================
# Configure 3 test users for instant role-based login in dev/staging.
# These are read by lib/devAuth.ts and shown as one-click login buttons on localhost.
#
# SETUP:
# 1. Enable "Email" provider in Supabase Dashboard → Auth → Providers
# 2. Run supabase/seed-test-users.sql in your Supabase SQL Editor
# 3. Copy this section to .env.local (gitignored) and fill in passwords
# 4. Restart dev server: npm run dev
#
# First login for each test user will go through OAuthSetup to set their persona.
# After that, dev login is instant — one click to any role.

# Manager / Agency Owner — routes to /admin dashboard
VITE_TEST_MANAGER_EMAIL=
VITE_TEST_MANAGER_PASSWORD=

# Technician — routes to /contractor portal
VITE_TEST_TECH_EMAIL=
VITE_TEST_TECH_PASSWORD=

# Solo Contractor — routes to /contractor portal (self-employed mode)
VITE_TEST_SOLO_EMAIL=
VITE_TEST_SOLO_PASSWORD=
